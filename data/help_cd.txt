cd: cd [dir]*
    Change the shell working directory.

    Change the current directory to DIR.  The default DIR is the value of the
    HOME shell variable.

    If a file or folder has a space separator, path should be pass as
    example: /folder/Space\ name
    where 'Space name' is the folder or file name.